
/* add hooks to check that once deselected these hooks won't run */

describe("test 4 - pass", () => {
    // this is a comment
    it("test 4 - pass", () => {
        expect(true).to.equal(false);
    });

    }
);

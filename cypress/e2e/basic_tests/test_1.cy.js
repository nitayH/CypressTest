
/* add hooks to check that once deselected these hooks won't run */

describe("test 1 - pass", () => {
    // this is a comment
    it("test 1 - pass", () => {
        expect(true).to.equal(true);
    });

    }
);
